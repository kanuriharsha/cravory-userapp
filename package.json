{
  "name": "cravory-monorepo-scripts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "eslint-config-expo": "~10.0.0",
    "typescript": "~5.9.2"
  }
}
